# AOF v2.5 Release Checklist

Date: `2026-06-14`

- package version: `2.5.0`
- current candidate commit SHA: `TBD (uncommitted release candidate worktree)`

## Gate 1: Allocation Plans Are First-Class

- [x] `allocation-plan-record` exists
- [x] allocation plan artifact is schema-backed
- [x] target roles, candidate resources, recommended allocations, and risk notes are persisted

Current reading:

- `src/commands/allocation-plan-record.js` writes canonical allocation plan artifacts
- `schemas/aof-allocation-plan.schema.json` validates the packet shape
- CLI exposes target role refs, candidate resource refs, recommended allocation JSON, policy refs, and risk notes

## Gate 2: Policy Evaluation Is First-Class

- [x] `policy-evaluation-report` exists
- [x] policy evaluation artifact is schema-backed
- [x] overall outcome and per-policy results are persisted

Current reading:

- `src/commands/policy-evaluation-report.js` writes policy evaluation artifacts
- `schemas/aof-policy-evaluation-report.schema.json` fixes the report contract
- CLI exposes policy refs, result JSON, overall outcome, and recommended actions

## Gate 3: Resource Claims Are First-Class

- [x] `resource-claim-record` exists
- [x] resource claim artifact is schema-backed
- [x] claim scope, claim status, approval policy refs, and justification are persisted

Current reading:

- `src/commands/resource-claim-record.js` writes explicit claim state artifacts
- `schemas/aof-resource-claim.schema.json` validates the claim packet shape
- CLI exposes approval policy refs, allocation plan refs, policy evaluation refs, and optional expiry

## Gate 4: Allocation Layer Is Coherent

- [x] allocation plan, policy evaluation, and resource claim are all runtime artifacts
- [x] CLI routes exist for all three commands
- [x] `roadmap-status` points to the current release definition
- [x] `metrics-snapshot` exposes allocation volume and review load

Current reading:

- `src/commands/allocation-artifact-helpers.js` unifies the allocation artifact root
- `src/cli.js` documents and dispatches all three v2.5 commands
- `src/commands/roadmap-status.js` now maps allocation / policy evaluation / resource claim work into the `v2.5` track and points to `docs/v2.5-release-definition.md`
- `src/commands/metrics-snapshot.js` now reports allocation-plan count, policy-evaluation count, resource-claim count, approval-required allocations, and open-claim review load

## Gate 5: Existing Runtime Baseline Remains Green

- [x] `organization-audit` remains green
- [x] `organization-verify` remains green
- [x] `npm test` remains green
- [x] `npm run smoke` remains green

Current reading:

- `npm test` completed successfully with `133 pass / 0 fail`
- `organization-verify --project .` completed successfully with `83 pass / 0 fail`
- `organization-audit --project .` completed successfully with `83 organization checks pass`, `1 decision fallback check pass`, and `0 duplicate task ids`
- `npm run smoke` completed successfully on `2026-06-14` after the v2.5 metrics and CLI-isolation updates

## Gate 6: Boundary Remains Honest

- [x] `v2.5.0` does not claim autonomous scheduling
- [x] `v2.5.0` does not claim runtime policy enforcement
- [x] `v2.5.0` does not claim `v3.0` loop proof

Current reading:

- `docs/v2.5-release-definition.md` keeps scope at recommendation / evaluation / claim artifacts
- `docs/v2.5-allocation-policy-definition.md` explicitly stops before autonomous staffing and enforcement

## Verification Snapshot

- [x] direct allocation / policy / claim command verification
- [x] `node ./src/cli.js metrics-snapshot --project .`
- [x] `node ./src/cli.js organization-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

Captured verification notes:

- direct CLI smoke verification succeeded for `allocation-plan-record`, `policy-evaluation-report`, and `resource-claim-record`
- a parser bug for `--policy-ref` was found during smoke execution and fixed before this checklist revision
- `metrics-snapshot` now reports allocation-plan count, policy-evaluation count, resource-claim count, approval-required allocation count, allocation-review-load, and open-resource-claim count
- `roadmap-status --project .` now points to `docs/v2.5-release-definition.md`, shows `TASK-010` as done, advances the next value slice to `TASK-011`, and keeps `TASK-011` in the `v3.0` track despite allocation wording in its description
- one intermittent standalone CLI SyntaxError referencing `roadmap-status.js` was observed and tracked as `TASK-024`; the CLI blast radius was mitigated by lazy-loading `roadmap-status` so unrelated commands no longer depend on that import path
