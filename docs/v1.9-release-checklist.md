# AOF v1.9 Release Checklist

`v1.9` を release-ready と判定する前に確認する checklist。  
この文書は [docs/v1.9-release-definition.md](./v1.9-release-definition.md) の gate を、repo 内の evidence に落としたものとする。

## Gate Checklist

### Gate 1: v1.8 Integrity Is Preserved

Required evidence:

- [x] task ledger, goal projection, archivist scope, and self-audit semantics are preserved
- [x] self-hosting additions do not weaken `v1.8` memory guarantees

Current reading:

- satisfied

### Gate 2: Main Lifecycle Commands Update Project Memory

Required evidence:

- [x] `run` updates current operating goal projection
- [x] `answer` updates recent confirmation memory and refined operating goal
- [x] `outcome-report` updates next value slice projection
- [x] `signal`, `escalation-resolve`, and approval-stage `council-exec` update recent confirmation memory
- [x] update behavior is covered by tests or equivalent operator verification

Current reading:

- satisfied

### Gate 3: Goal Loop Is Maintained In Self-Hosting Operation

Required evidence:

- [x] `North Star Goal`, `Current Operating Goal`, and `Next Value Slice` all exist under `.aof/goals/`
- [x] role of each goal layer is explicit in docs
- [x] completion and next-slice refresh behavior are explicit

Current reading:

- satisfied

### Gate 4: Recent Confirmation Window Works As Repeated Confirmation Memory

Required evidence:

- [x] repeated core questions are supported
- [x] recent-window retention rule is explicit
- [x] alignment / mismatch / scale direction are preserved in artifact form
- [x] at least one current self-hosting confirmation window reflects the latest operating truth

Current reading:

- satisfied

### Gate 5: Alignment Pulse And Task Triage Are Continuous

Required evidence:

- [x] alignment pulse is defined as continuing operation, not just occasional review
- [x] task triage is part of the pulse
- [x] stale task handling is explicit
- [x] relation between pulse outputs and recent confirmation memory is explicit

Current reading:

- satisfied

### Gate 6: Self-Audit Is Part Of The Operating Loop

Required evidence:

- [x] at least one self-audit evidence artifact exists under the new loop
- [x] self-audit can create or refresh canonical AOF tasks
- [x] human-readable summary of the detected gap exists

Current reading:

- satisfied

## Current Readiness Snapshot

- current commit SHA: `to be refreshed after current save`
- current public package version: `1.9.0`
- CI evidence: pending
