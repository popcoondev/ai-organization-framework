# AOF v2.1.0 RC Draft

- release candidate: `v2.1.0-rc1`
- candidate commit SHA: `80352ea`
- status: draft

## Summary

`v2.1.0` is planned as the release that makes AOF's organization model first-class.

The release surface centers on:

- AOF as AI Organization Operating System
- `.aof/organization.json`
- role / agent separation
- council / team / contract / dependency / knowledge / metrics / lifecycle representation
- installer and upgrade support for organization manifests

## Current Reading

- candidate surface is fixed at `80352ea`
- local evidence:
  - `npm test` passed on the release candidate branch
  - `node --check src/runtime/project-memory.js`
  - `node --check src/commands/init-project.js`
  - `node --check src/cli.js`
  - direct `init -> organization manifest -> upgrade preservation` smoke passed
- remaining release work:
  - CI confirmation
  - sign-off
