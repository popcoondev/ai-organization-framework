# AOF v4.0 Release Checklist

Date: `2026-06-19`

- target release version: `4.0.0`
- current package version: `4.0.0`
- latest stable tagged release before this cut: `v3.9.0`
- sign-off: `popcoondev / Codex`
- decision date: `2026-06-19`

## Gate 1: Human Recognition Interface Exists

- [x] the runtime can show on one screen what is happening now
- [x] the runtime can show where this work sits in the trunk / branch / frontier tree
- [x] the runtime can show who is acting and who is judging
- [x] the runtime can show what should happen next

Current evidence:

- `src/commands/visibility-serve.js`
- `src/commands/visibility-export.js`
- `docs/v4.0-human-recognition-interface-spec.md`

## Gate 2: Canonical Mapping Exists

- [x] artifact -> packet -> interface mapping is documented
- [x] character / roadmap / timeline / blocker / next-action surfaces have named artifact sources
- [x] the interface spec is canonical rather than implied by code only

Current evidence:

- `docs/v4.0-human-recognition-interface-spec.md`
- `docs/v4.0-direction-runtime-review.md`

## Gate 3: Runtime-Backed Fact Is Visible

- [x] the interface shows runtime-backed status explicitly
- [x] the interface shows last runtime execution time and command chain
- [x] the interface shows refreshed artifact refs
- [x] missing runtime backing visibly weakens the claim
- [x] generic proof success cannot mask a mismatch with the visible frontier state

Current evidence:

- `.aof/artifacts/visibility/current/runtime-execution.json`
- `src/commands/visibility-export.js`
- `src/commands/visibility-serve.js`

## Gate 4: Live Organization Loop Is Legible

- [x] the interface shows council invocation
- [x] the interface shows council seat sequence and judgment
- [x] the interface shows workforce assembly and returned outputs
- [x] the interface shows council review and orchestrator return
- [x] the visible loop is traceable back to the active frontier task

Current evidence:

- `.aof/sessions/SESS-MQJSNCBI-UQJ0NR.json`
- `.aof/artifacts/execution/role-joins/RJOIN-MQJSNCCD-NJ04BZ.json`
- `.aof/artifacts/execution/team-outputs/TOUT-MQJSNCCE-3BRLH7.json`
- `.aof/artifacts/execution/council-reviews/CREV-MQJSNCCF-UDA7SO.json`

## Gate 5: Human Recognition Beats Static Visibility

- [x] the interface shows timeline / what changed
- [x] the interface shows blockers and next action
- [x] the interface shows the current frontier as a bounded branch above shipped trunk
- [x] the release creates a visible recognition upgrade over the `v3.9` surface

Current evidence:

- `.aof/artifacts/visibility/current/timeline-feed.json`
- `.aof/artifacts/visibility/current/tree-position.json`
- `.aof/artifacts/visibility/current/evidence-drill-down.json`
- `src/commands/visibility-serve.js`

## Gate 6: Boundary Remains Honest

- [x] every visible state is artifact-derived
- [x] Human Recognition Packet remains intermediate layer rather than authority
- [x] Discovery remains evidence
- [x] Need Validation remains approval authority

Current evidence:

- `docs/v4.0-release-definition.md`
- `docs/v4.0-human-recognition-interface-spec.md`
- `docs/aof-operations-model.md`

## Verification Snapshot

- [x] `node ./src/cli.js situation-assess --project .`
- [x] `node ./src/cli.js operator-brief --project .`
- [x] `node ./src/cli.js visibility-export --project .`
- [x] `node ./src/cli.js visibility-session --project . --port 4178`
- [x] `node ./src/cli.js runtime-loop-proof --project . --provider mock --deep-path --source-task-id TASK-046`
- [x] `node ./src/cli.js release-state-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

Verification evidence:

- `situation-assess` passed after the release transition, reporting `active_release_version = 4.0.0`, `primary_frontier_task = TASK-047`, `track = v5.0`, and no truth conflicts.
- `operator-brief` passed and emitted `.aof/artifacts/visibility/current/operator-brief.json`, stating `TASK-047 is the live v5.0 frontier after 4.0.0.`
- `visibility-export` passed and refreshed the full operator packet set, including `mission-control.json`, `operator-brief.json`, `operator-progress.json`, `tree-position.json`, `evidence-drill-down.json`, and `runtime-execution.json`.
- `visibility-session --port 4178` started a real local viewer session; the in-app browser confirmed `AOF Human Recognition Interface`, `Shipped trunk: v4.0`, `Live branch: v5.0`, `Frontier: TASK-047`, and `Is this actually runtime-backed? Yes`.
- `runtime-loop-proof --source-task-id TASK-046` passed and refreshed `.aof/artifacts/runtime-loop-proofs/current-proof.json`, preserving the release-defining v4.0 loop proof chain.
- `release-state-audit` passed for active release `4.0.0`.
- `organization-verify` passed `1695 / 1695` checks.
- `npm test` passed in the Node 22 lane.
- `npm run smoke` passed in the Node 22 lane.

## Release Decision

Current decision: **signed off**.

Sign-off basis:

- the one-screen Human Recognition Interface is visibly present
- the live runtime loop is readable from the same interface
- the loop shown on screen is the active frontier loop, not merely a generic proof pass
- runtime-backed fact is explicit and artifact-backed
- canonical verification lanes are green
