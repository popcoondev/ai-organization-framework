# AOF v2.2 Release Checklist

Date: `2026-06-13`

- package version: `2.2.0`
- current candidate commit SHA: `27bb8dc`

## Gate 1: Skill Is First-Class

- [x] AOF docs define a first-class skill model
- [x] skill identity, owner, version, applicable roles, and expected outputs are representable
- [x] skills are not treated only as prompt conventions

Current reading:

- docs exist in `docs/aof-skill-model.md`
- schema exists in `schemas/aof-skills.schema.json`
- runtime bootstrap writes `.aof/skills.json`

## Gate 2: Capability Is Typed

- [x] capability categories are defined
- [x] capability registry exists beyond free-form strings
- [x] role / agent / skill relationships to capabilities are expressible

Current reading:

- docs exist in `docs/aof-capability-model.md`
- schema exists in `schemas/aof-capability-registry.schema.json`
- role / agent / skill / resource links are validated by `organization-verify`

## Gate 3: Resource Inventory Exists

- [x] resource artifact exists
- [x] agents, tools, MCP servers, repositories, environments, and budget/token envelopes are representable
- [x] resource identity is separated from scheduling logic

Current reading:

- docs exist in `docs/aof-resource-model.md`
- schema exists in `schemas/aof-resource-inventory.schema.json`
- scheduling remains intentionally deferred

## Gate 4: Policy Is Machine-Readable

- [x] approval rules are representable
- [x] escalation rules are representable
- [x] delegation and access rules are representable
- [x] policy exists as artifact, not only prose

Current reading:

- docs exist in `docs/aof-policy-model.md`
- schema exists in `schemas/aof-policy.schema.json`
- runtime bootstrap writes `.aof/policies.json`

## Gate 5: Validation Is Productized

- [x] validation command exists for organization checks
- [x] validation command exists for contract consistency checks
- [x] validation command exists for dependency reference checks
- [x] validation command exists for artifact completeness checks
- [x] validation command exists for decision checks

Current reading:

- `organization-verify` is productized and green
- `decision-verify` is productized for standalone decision artifact checks
- unified benchmark validation surface is not complete yet

## Gate 6: Metrics And Analytics Are Inspectable

- [x] basic metrics snapshots exist
- [x] basic organization analytics snapshots exist
- [x] outputs can be reviewed without a full analytics backend

Current reading:

- `metrics-snapshot` writes a first-class metrics artifact
- `organization-analytics-snapshot` writes a first-class analytics artifact
- both surfaces are inspectable as local JSON without an analytics backend

## Gate 7: Learning Loop Is Formalized

- [x] outcome reports, self-audits, and improvement proposals are explicitly connected
- [x] learning-loop artifacts exist as inspectable records
- [x] the loop is formalized without claiming autonomous adaptation

Current reading:

- `learning-loop-snapshot` writes a first-class artifact joining outcome evidence, self-audit, next value slice, and improvement focus
- the artifact is inspectable only; it does not claim autonomous policy or org rewrites

## Gate 8: v3.x Boundary Remains Clean

- [x] `v2.2` docs do not claim autonomous workforce management
- [x] resource scheduling remains deferred
- [x] runtime policy enforcement remains deferred
- [x] adaptive optimization remains deferred

Current reading:

- release definition and integration-boundary docs keep `v2.2 = represent / validate / inspect`
