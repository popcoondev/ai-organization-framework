# AOF v2.4 Release Checklist

Date: `2026-06-14`

- package version: `2.4.0`
- current candidate commit SHA: `TBD (uncommitted release candidate worktree)`

## Gate 1: Role Results Are First-Class

- [x] `role-result-record` exists
- [x] role result artifact is schema-backed
- [x] status / rationale / recommendation / signals / decision-required are persisted

Current reading:

- `src/commands/role-result-record.js` writes canonical role result artifacts
- `schemas/aof-role-result.schema.json` validates the packet shape
- CLI exposes `--session-id`, `--status`, `--recommendation`, `--rationale`, `--signal`, `--decision-required`, and lineage source refs

## Gate 2: Team Output Is First-Class

- [x] `team-output-record` exists
- [x] expected / received / missing roles are persisted
- [x] joined role result refs and next-step guidance are persisted

Current reading:

- `src/commands/team-output-record.js` derives `missing_roles` when omitted
- `schemas/aof-team-output.schema.json` fixes the packet boundary
- aggregate state is constrained to reviewable execution states

## Gate 3: Council Review Is First-Class

- [x] `council-review-packet` exists
- [x] review status / decision summary / recommendation are persisted
- [x] follow-up task ids and escalation requirement are persisted

Current reading:

- `src/commands/council-review-packet.js` writes council judgment artifacts
- `schemas/aof-council-review-packet.schema.json` fixes the judgment packet contract
- CLI exposes evidence refs and follow-up task ids without raw JSON editing

## Gate 4: Execution Lineage Is Inspectable

- [x] `execution-lineage` exists
- [x] role / team / council execution packets can be aggregated
- [x] lineage can be filtered by source task id, parent session id, and stage
- [x] active lineage snapshot is written

Current reading:

- `src/commands/execution-lineage.js` aggregates execution artifacts into `.aof/context/active/execution-lineage.json`
- lineage reports counts, stages observed, blocking signal count, and recommended next step
- the runtime test suite covers the role -> team -> council -> lineage path

## Gate 5: Existing Operator Surface Remains Green

- [x] `organization-audit` remains available
- [x] `organization-verify` remains green
- [x] `npm test` remains green
- [x] `npm run smoke` remains green

Current reading:

- `npm test` completed successfully with `121 pass / 0 fail`
- `npm run smoke` completed successfully against package version `2.4.0`
- direct execution-contract command verification succeeded for role, team, council, and lineage artifacts

## Gate 6: Boundary Remains Honest

- [x] `v2.4.0` does not claim allocation runtime
- [x] `v2.4.0` does not claim policy enforcement runtime
- [x] `v2.4.0` does not claim backend-specific orchestration automation
- [x] `v2.5` and `v3.0` remain separate roadmap steps

Current reading:

- `docs/v2.4-release-definition.md` keeps scope at execution packet contracts and lineage
- allocation planning, policy evaluation, and backend-neutral loop proof remain assigned to `TASK-010` and `TASK-011`

## Verification Snapshot

- [x] direct role -> team -> council -> lineage command verification
- [x] `node ./src/cli.js organization-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

Captured verification notes:

- execution packet commands were validated end-to-end through a temporary initialized project
- `organization-audit` remained green after contract and release-doc updates
- `organization-verify` remained green after the governance release contract ref switched to `docs/v2.4-release-definition.md`
- `npm test` completed successfully with `121 pass / 0 fail`
- `npm run smoke` completed successfully
