# AOF v2.0.0 RC Draft

- release candidate: `v2.0.0-rc1`
- candidate commit SHA: `29b8c2a`
- status: `ready`

## Summary

`v2.0.0` is planned as the release that reduces AOF documentation to a small canonical set and makes installer/bootstrap the main entrypoint for managed projects.

The release surface centers on:

- canonical doc reduction
- `aof init` bootstrap
- `aof upgrade` migration
- explicit managed-project default
- a concrete acquisition flow from GitHub release/tag to local install

## Current Reading

- candidate surface is fixed at `29b8c2a`
- local evidence:
  - `npm test` passed on the release candidate branch
  - `node --check src/runtime/project-memory.js`
  - `node --check src/commands/init-project.js`
  - `node --check src/commands/upgrade-project.js`
  - `node --check src/cli.js`
  - direct `init -> downgrade manifest -> upgrade` smoke passed
- CI evidence: [GitHub Actions run 26985419204](https://github.com/popcoondev/ai-organization-framework/actions/runs/26985419204)
- sign-off: `popcoondev / popcoondev`
- decision date: `2026-06-05`
- candidate outcome: ready
- remaining release work: publishing
