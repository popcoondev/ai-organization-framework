# AOF v3.0 Release Checklist

Date: `2026-06-14`

- target release version: `3.0.0`
- current package version: `2.6.0`
- current candidate commit SHA: `pending v3.0 packaging commit (worktree based on 2384bef)`

## Gate 1: Parent/Child Orchestration Is Contracted

- [x] `role-result-record` exists
- [x] `role-join-record` exists
- [x] `team-output-record` exists
- [x] `execution-lineage` includes role join state

## Gate 2: Allocation And Policy Participate In Runtime

- [x] runtime proof creates an allocation plan artifact
- [x] runtime proof creates a policy evaluation artifact
- [x] runtime proof creates a resource claim artifact
- [x] allocation and policy artifacts are linked into execution and review evidence

## Gate 3: One End-To-End Loop Is Auditable

- [x] `runtime-loop-proof` exists
- [x] one proof run records framing, allocation, execution, review, outcome, and next-step recommendation
- [x] proof output includes canonical refs for every major artifact transition

## Gate 4: Self-Hosting Repo Can Run The Loop

- [x] root `.aof` contains the workflow/template manifest set needed by `run` and `council-exec`
- [x] `runtime-loop-proof --project . --provider mock --source-task-id TASK-011` succeeds on the self-hosting repo
- [x] `roadmap-status` points to `docs/v3.0-release-definition.md`

## Gate 5: Existing Runtime Baseline Remains Green

- [x] `organization-audit` remains green
- [x] `organization-verify` remains green
- [x] `npm test` remains green
- [x] `npm run smoke` remains green

## Gate 6: Boundary Remains Honest

- [x] `v3.0.0` claims one auditable runtime loop, not autonomous workforce management
- [x] `v3.0.0` does not claim always-on daemon autonomy
- [x] `v3.0.0` does not claim broad provider-family production parity beyond the contract layer

## Verification Snapshot

- [x] `node ./src/cli.js runtime-loop-proof --project . --provider mock --source-task-id TASK-011`
- [x] `node ./src/cli.js roadmap-status --project .`
- [x] `node ./src/cli.js organization-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm test -- --runInBand`
- [x] `npm run smoke`

Captured verification notes:

- `runtime-loop-proof` generated `.aof/artifacts/runtime-loop-proofs/current-proof.json`
- proof refs include allocation, policy evaluation, resource claim, role results, role join, team output, council review, execution lineage, and learning loop artifacts
- `organization-verify --project .`: `83 pass / 0 fail`
- `organization-audit --project .`: green, duplicate task count `0`
- `npm test -- --runInBand`: `138 pass / 0 fail`
- `npm run smoke`: exited `0`, `approvalStatus: "approved"`, `latestOutcomeResult: "success"`
- `npm run smoke` also surfaced verify drift warnings: `verifyLineageHealthStatus: "warning"`, `verifyArchiveIndexHealthStatus: "critical"`, recommended actions `investigate-lineage-drift` and `human-review-recommended`
- follow-up runtime bug is tracked as `TASK-028` for intermittent Node `v25.5.0` import truncation during parallel standalone CLI runs
- root `.aof/aof.yaml` and companion actor/workflow/template files now let the self-hosting repo execute `run`-family workflow surfaces directly
- package version has not been bumped to `3.0.0` yet; this checklist is for sign-off packaging before final release cut
