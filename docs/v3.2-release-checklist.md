# AOF v3.2 Release Checklist

Date: `2026-06-16`

- target release version: `3.2.0`
- current package version: `3.2.0`
- latest stable tagged release: `v3.1.0`
- current candidate commit SHA: `to be recorded by the final release tag`

## Gate 1: Need Validation Artifact Surface Exists

- [x] problem statement schema and command exist
- [x] value hypothesis schema and command exist
- [x] alternative analysis schema and command exist
- [x] experiment proposal schema and command exist
- [x] project charter schema and command exist
- [x] need validation record schema and command exist

Current evidence:

- `schemas/aof-problem-statement.schema.json`
- `schemas/aof-value-hypothesis.schema.json`
- `schemas/aof-alternative-analysis.schema.json`
- `schemas/aof-experiment-proposal.schema.json`
- `schemas/aof-project-charter.schema.json`
- `schemas/aof-need-validation-record.schema.json`
- `src/commands/problem-statement-record.js`
- `src/commands/value-hypothesis-record.js`
- `src/commands/alternative-analysis-record.js`
- `src/commands/experiment-proposal-record.js`
- `src/commands/project-charter-record.js`
- `src/commands/need-validation-record.js`

## Gate 2: Pre-Project Runtime Gate Exists

- [x] framed sessions stop at `need-validation`
- [x] `council-exec` rejects later stages before the gate is advanced
- [x] clarification completion emits a need validation decision

Current evidence:

- `schemas/aof-session.schema.json`
- `src/runtime/session.js`
- `src/runtime/decision.js`
- `src/commands/answer.js`
- `src/commands/council-exec.js`
- `test/runtime.test.js`

## Gate 3: Controlled Advance Into Planning Exists

- [x] `need-validation-advance` requires an approvable need validation record
- [x] `need-validation-advance` requires a linked project charter
- [x] operating goal projection is deferred until planning starts
- [x] approved sessions advance into `planning`

Current evidence:

- `src/commands/need-validation-advance.js`
- `src/runtime/session.js`
- `src/commands/answer.js`
- `test/runtime.test.js`

## Gate 4: Benchmark Surface Exists

- [x] `need-validation-benchmark` evaluates `NV-001` through `NV-006`
- [x] benchmark output is writable as a machine-readable artifact
- [x] blocked and project-ready cases are both representable in the contract

Current evidence:

- `src/commands/need-validation-benchmark.js`
- `docs/aof-need-validation-model.md`
- `test/runtime.test.js`

## Gate 5: Docs And Runtime Story Agree

- [x] core model declares Need Validation as mandatory pre-project gate
- [x] operations model lists the Need Validation commands
- [x] Discovery docs hand off into Need Validation
- [x] README exposes the Need Validation model and `v3.2` release surface

Current evidence:

- `README.md`
- `docs/aof-core-model.md`
- `docs/aof-operations-model.md`
- `docs/aof-need-validation-model.md`
- `docs/v3-discovery-artifact-definition.md`
- `docs/v3.2-release-definition.md`

## Gate 6: Baseline Runtime Safety Remains Green

- [x] `npm test -- --runInBand` remains green
- [x] `node ./src/cli.js organization-verify --project .` remains green
- [x] `runtime-loop-proof` still succeeds after the new gate is added
- [x] `live-verify` still succeeds after the new gate is added

Current evidence:

- `npm test -- --runInBand`: `151 pass / 0 fail`
- `node ./src/cli.js organization-verify --project .`: `289 pass / 0 fail`
- `src/commands/runtime-loop-proof.js`
- `src/commands/live-verify.js`
- `test/runtime.test.js`

## Verification Snapshot

- [x] `npm test -- --runInBand`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `node ./src/cli.js need-validation-benchmark --project .`
- [x] `node ./src/cli.js runtime-loop-proof --project . --provider mock --source-task-id TASK-011`
- [x] `live-verify` succeeds on the validated Node 22 lane with the committed signal fixture

Captured verification notes:

- package metadata is now aligned to `3.2.0`
- Need Validation command surface and runtime gate are implemented and tested
- `npm test -- --runInBand`: `151 pass / 0 fail`
- `node ./src/cli.js organization-verify --project .`: `289 pass / 0 fail`
- `node ./src/cli.js need-validation-benchmark --project .`: pass with committed `NV-001` to `NV-006` benchmark fixtures
- `node ./src/cli.js runtime-loop-proof --project . --provider mock --source-task-id TASK-011`: pass
- `live-verify` is unstable on local Node 25 and remains outside the validated lane, but it passes on Node 22 with `--artifact-dir /tmp/aof-live-verification-v320-node22 --include-middle-stages --include-approval --include-signal-reopen --include-escalation-reopen --include-escalation-terminal --signal-path ./.aof/signals/SIG-001.json`

## Release Decision

Current decision: **candidate-ready, not yet taggable**.

`v3.2.0` becomes taggable only after:

1. the final candidate commit SHA is recorded by the release tag and reflected in the release surface
