# AOF v3.9 Release Checklist

Date: `2026-06-18`

- target release version: `3.9.0`
- current package version: `3.9.0`
- latest stable tagged release before this cut: `v3.8.0`
- sign-off: `popcoondev / Codex`
- decision date: `2026-06-18`

## Gate 1: Progress Surface Exists

- [x] a dedicated `operator-progress` command exists
- [x] the progress packet explains what changed since the last meaningful checkpoint
- [x] the packet stays tied to the live frontier task

Current evidence:

- `src/commands/operator-progress.js`
- `schemas/aof-operator-progress-view.schema.json`

## Gate 2: Tree Position Exists

- [x] a dedicated `tree-position` command exists
- [x] the tree packet states the shipped trunk and live frontier branch
- [x] the tree packet stays aligned with live release truth

Current evidence:

- `src/commands/tree-position.js`
- `schemas/aof-tree-position-view.schema.json`

## Gate 3: Proof Surface Exists

- [x] a dedicated `evidence-drill-down` command exists
- [x] the packet explains why the current headline, blockers, and next action are true
- [x] the packet stays bounded to canonical runtime refs

Current evidence:

- `src/commands/evidence-drill-down.js`
- `src/commands/evidence-drill-down-benchmark.js`
- `schemas/aof-evidence-drill-down-view.schema.json`

## Gate 4: Viewer Session Becomes One Runtime Path

- [x] `visibility-session` exports the packet and starts the viewer session
- [x] browser open can be triggered from the same runtime command
- [x] the session passes progress / tree / proof packet refs into the viewer path

Current evidence:

- `src/commands/visibility-session.js`
- `src/commands/visibility-export.js`
- `src/commands/visibility-serve.js`

## Gate 5: Viewer Shows Real Progress

- [x] the viewer can show `What Changed`
- [x] the viewer can show `Where In The Tree`
- [x] the viewer can show `Evidence Drill-Down`
- [x] the release creates a visible surface upgrade over the static v3.8 path

Current evidence:

- `src/commands/visibility-serve.js`
- `test/runtime-core-2.test.js`
- `docs/quickstart.md`

## Gate 6: Boundary Remains Honest

- [x] progress / tree / proof packets remain artifact-derived
- [x] the viewer does not become the primary truth source
- [x] Need Validation remains approval authority
- [x] Discovery remains evidence

Current evidence:

- `docs/v3.9-release-definition.md`
- `docs/v3.9-direction-runtime-review.md`
- `docs/v3.9.0-release-notes.md`

## Verification Snapshot

- [x] `node ./src/cli.js operator-progress --project .`
- [x] `node ./src/cli.js tree-position --project .`
- [x] `node ./src/cli.js evidence-drill-down --project .`
- [x] `node ./src/cli.js visibility-export --project .`
- [x] `node ./src/cli.js visibility-session --project . --port 4176`
- [x] `node ./src/cli.js evidence-drill-down-benchmark --project .`
- [x] `node ./src/cli.js situation-assess --project .`
- [x] `node ./src/cli.js release-state-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

Verification evidence:

- `operator-progress` passed and emitted `.aof/artifacts/visibility/current/operator-progress.json`, showing `TASK-045` as the shipped release frontier and `TASK-044` as the previous checkpoint during the release cut.
- `tree-position` passed and emitted `.aof/artifacts/visibility/current/tree-position.json`, proving the shipped trunk / live branch model and then the post-release transition to `TASK-046` 