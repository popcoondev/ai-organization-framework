# AOF v2.6 Release Checklist

Date: `2026-06-14`

- package version: `2.6.0`
- current candidate commit SHA: `pending release commit (worktree based on 0fc515f)`

## Gate 1: Status Card Is Runtime-Backed

- [x] `visibility-export` exists
- [x] generated `status_card` is schema-backed
- [x] current phase, current goal, next checkpoint, and evidence state are exported from live runtime state

## Gate 2: Timeline Feed Is Runtime-Backed

- [x] generated `timeline_feed` is schema-backed
- [x] entries are derived from runtime artifacts rather than fixture-only input
- [x] timeline entries expose canonical `.aof` refs

## Gate 3: Flow Snapshot Is Runtime-Backed

- [x] generated `flow_snapshot` is schema-backed
- [x] ordered nodes expose bridge-release progress
- [x] `visibility_projection` was represented as the current bridge-release phase while open and now transitions to `done` after v2.6 task closure

## Gate 4: Viewer Integration Remains Coherent

- [x] generated views are consumable by `visibility-serve`
- [x] `roadmap-status` exposes `v2.6` as a first-class release track
- [x] `roadmap-status` points to `docs/v2.6-release-definition.md`

## Gate 5: Existing Runtime Baseline Remains Green

- [x] `organization-audit` remains green
- [x] `organization-verify` remains green
- [x] `npm test` remains green
- [x] `npm run smoke` remains green

## Gate 6: Boundary Remains Honest

- [x] `v2.6.0` does not claim hosted visibility infrastructure
- [x] `v2.6.0` does not claim backend-neutral runtime proof
- [x] `v2.6.0` does not claim autonomous orchestration

## Verification Snapshot

- [x] `node ./src/cli.js visibility-export --project .`
- [x] `node ./src/cli.js roadmap-status --project .`
- [x] `node ./src/cli.js organization-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm test`
- [x] `npm run smoke`

Captured verification notes:

- runtime-backed visibility generation is implemented in `src/commands/visibility-export.js`
- roadmap classification now exposes `v2.6` visibility work separately from `v3.0`
- `organization-verify --project .`: `83 pass / 0 fail`
- `organization-audit --project .`: green, duplicate task count `0`
- `npm test -- --runInBand`: `136 pass / 0 fail`
- `npm run smoke`: completed successfully on `2026-06-14`
- `visibility-export` now yields `usage_level: "runtime-backed"` and advances the flow snapshot from `visibility_projection` to `runtime_loop_proof` after v2.6 closure
- direct local `visibility-serve` port binding remains sandbox-limited in this environment (`listen EPERM`), so compatibility evidence is taken from the shared loader contract and runtime test coverage
