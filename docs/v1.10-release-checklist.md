# AOF v1.10 Release Checklist

`v1.10` を release-ready と判定する前に確認する checklist。  
この文書は [docs/v1.10-release-definition.md](./v1.10-release-definition.md) の gate を、repo 内の evidence に落としたものとする。

## Gate Checklist

### Gate 1: v1.9 Cadence Runtime Integrity Is Preserved

Required evidence:

- [x] cadence timing / schedule / dispatch / profile artifacts remain intact
- [x] `v1.10` additions do not weaken the runtime-backed cadence loop

Current reading:

- satisfied

### Gate 2: Visibility Contract Can Carry Cadence Summary

Required evidence:

- [x] cadence summary fields are defined in `docs/human-visibility-output-formats.md`
- [x] cadence summary fields are optional and backward-compatible

Current reading:

- satisfied

### Gate 3: visibility-serve Reads Cadence Summary

Required evidence:

- [x] `visibility-serve` derives cadence summary from the status payload
- [x] viewer exposes cadence summary in the rendered page
- [x] runtime test covers cadence summary loading / rendering

Current reading:

- satisfied

### Gate 4: GitHub Operations Model Is Topology-Aware

Required evidence:

- [x] `docs/github-operations-model.md` defines self-hosting and managed-project topologies
- [x] managed-project default forbids direct cadence pushes to product `main`
- [x] recommended write targets for managed projects are explicit

Current reading:

- satisfied

### Gate 5: Self-Hosting GitHub Binding Is Hardened

Required evidence:

- [x] `.aof/**` cadence commits are excluded from full CI
- [x] cadence workflow has scoped staging and current concurrency guardrails
- [x] workflow/docs state that the current binding is self-hosting-topology-specific

Current reading:

- satisfied

### Gate 6: Self-Audit And Task Evidence Reflect The New Boundary

Required evidence:

- [x] self-audit evidence records cadence visibility closure
- [x] `TASK-005` is closed and the next gap is explicit
- [x] current task / goal / confirmation state reflects the new next move

Current reading:

- satisfied

## Current Readiness Snapshot

- current candidate commit SHA: `pending`
- current public package version: `1.9.0`
- local runtime evidence: `node --test test/runtime.test.js --test-name-pattern "visibility view loader and HTML shell align with the v1.4 visibility contract"` -> `98 pass, 0 fail`
- CI evidence: pending
