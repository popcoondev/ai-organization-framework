# AOF v2.3 Release Checklist

Date: `2026-06-14`

- package version: `2.3.0`
- current candidate commit SHA: `TBD (uncommitted release candidate worktree)`

## Gate 1: Organization State Is Operator-Visible

- [x] `organization-status` exists
- [x] goals, topology, councils, teams, roles, and task counts are visible from CLI
- [x] capability-layer presence is visible from CLI

Current reading:

- `node ./src/cli.js organization-status --project .` returns topology / goals / organization summary / task counts / capability-layer presence
- self-hosting candidate reports `open: 6 / done: 11 / archived: 1`

## Gate 2: Contracts And Dependencies Are First-Class Registers

- [x] `contract-register` exists
- [x] `dependency-graph` exists
- [x] contract artifact presence is visible without raw JSON inspection
- [x] dependency edges are visible without raw JSON inspection

Current reading:

- `contract-register` lists contract id / owner / artifact ref / artifact presence
- `dependency-graph` lists flat edges and adjacency
- governance release contract now points to `docs/v2.3-release-definition.md` and resolves `artifact_present: true`

## Gate 3: Decision And Roadmap State Are First-Class Registers

- [x] `decision-register` exists
- [x] `roadmap-status` exists
- [x] decision markdown/json pairing is visible from CLI
- [x] roadmap and runtime backlog mapping is visible from CLI

Current reading:

- `decision-register` returns decision id / title / stage / canonical markdown path / pair state
- `roadmap-status` groups runtime tasks into `v2.3` / `v2.4` / `v2.5` / `v3.0` / `unmapped`
- current self-hosting decision register reports `ADR-001` as `pair_alignment_state: aligned`

## Gate 4: Unified Audit Exists

- [x] `organization-audit` exists
- [x] organization validation summary is bundled for operator review
- [x] decision presence summary is bundled for operator review
- [x] task lifecycle duplicate detection is bundled for operator review
- [x] active audit artifact is written

Current reading:

- `organization-audit --project .` writes `.aof/context/active/organization-audit.json`
- the audit currently returns `ok: true`
- audit summary currently reports `organization_verify: 82/82 pass`, `decision_verify: 1/1 pass`, and `duplicate_task_count: 0`

## Gate 5: Runtime Integrity Is Tightened

- [x] concurrent task creation collisions are guarded
- [x] task updates consolidate duplicate lifecycle copies
- [x] historical duplicate task ids are detectable and were reconciled in self-hosting state

Current reading:

- `src/runtime/project-memory.js` now serializes task mutations and canonicalizes per-task lifecycle state
- `organization-audit` reports `duplicate_task_count: 0` after reconciling the historical `TASK-005` collision
- self-hosting runtime now preserves completed `TASK-005` history and tracks the former open collision as `TASK-018`

## Gate 6: v2.2 Baseline Remains Intact

- [x] organization / capability-layer validation still passes
- [x] `v2.3` does not claim execution packet runtime
- [x] `v2.3` does not claim allocation or policy enforcement
- [x] `v2.3` remains inside `represent / validate / inspect`

Current reading:

- `docs/v2.3-release-definition.md` keeps scope at operator surface and unified audit
- `docs/v2.3-operator-surface-definition.md` explicitly defers execution packet runtime, allocation, enforcement, and autonomous orchestration

## Verification Snapshot

- [x] `node ./src/cli.js organization-status --project .`
- [x] `node ./src/cli.js contract-register --project .`
- [x] `node ./src/cli.js dependency-graph --project .`
- [x] `node ./src/cli.js decision-register --project .`
- [x] `node ./src/cli.js roadmap-status --project .`
- [x] `node ./src/cli.js organization-audit --project .`
- [x] `node ./src/cli.js organization-verify --project .`
- [x] `npm run smoke`
- [x] `npm test`

Captured verification notes:

- `organization-audit` remained green after release-doc updates
- `organization-verify` remained green after the governance contract ref switched to `docs/v2.3-release-definition.md`
- `npm run smoke` completed successfully against package version `2.3.0`
- `npm test` completed successfully with `117 pass / 0 fail`
